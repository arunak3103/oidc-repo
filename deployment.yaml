apiVersion: apps/v1
kind: Deployment
metadata:
  name: static-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: static-app
  template:
    metadata:
      labels:
        app: static-app
    spec:
      containers:
      - name: static-app
        image: 955030484275.dkr.ecr.us-east-1.amazonaws.com/static-app:v1
        ports:
        - containerPort: 80
